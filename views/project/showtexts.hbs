



<h2 class="entered-text-display-header"><strong>{{projectname}}</strong> ({{client}}):</h2>

<p class="project-log">Language: <strong><span>{{language}}</span></strong>, Author: <strong>{{author}}</strong>, Last updated on <strong>{{date updateDate "DD/MM/YYYY"}}</strong> at <strong>{{date updateDate "HH:mm"}}</strong>.</p>
        <div class="statusses">
        <button class="status-{{status}}">
        {{status}}</button>

            <div class="status-updates">

            <form method="POST" action="/project/{{_id}}/completed">
            <button class="finished-button"> ✓ completed!</button>
            </form>

            <form method="POST" action="/project/{{_id}}/ongoing">
            <button class="ongoing-button"> x ongoing</button>
            </form>

            </div>

        </div>
<br>



    {{#each data}}



    <p class= "display-{{this.texttype}}"> {{this.textarea}}</p>
    <small class="marking-texttype">({{this.texttype}})</small>
    <br>
    <br>


    {{/each}}

<a class="add-texts-button" href="/project/{{_id}}/add">Add Texts</a>
<a class="add-texts-button" href="/project/{{_id}}/edit">Edit existing texts</a>
